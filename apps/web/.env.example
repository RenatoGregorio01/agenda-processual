# Browser
NEXT_PUBLIC_API_URL=http://localhost:8000
# staging = faixa de homologação no topo (homelab develop)
# NEXT_PUBLIC_APP_ENV=staging

# Server (opcional; no Docker Compose use http://api:8000)
API_URL=http://localhost:8000
